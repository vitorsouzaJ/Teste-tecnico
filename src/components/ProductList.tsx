import React from 'react'
import { ProductListContainer } from './layout/styles'

const ProductList: React.FC = () => {
  return (
    <ProductListContainer>
      <h2>Lista de Produtos</h2>
    </ProductListContainer>
  )
}

export default ProductList
